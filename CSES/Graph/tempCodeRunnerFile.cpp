 // queue<int> ans;
            // ans.push(cur);
            // int k = dis[cur];
            // cout << k << "\n";
            // while (k)
            // {
            //     int p = ans.front();
            //     ans.pop();
            //     res.pb(p);
            //     for (int i = 0; i < adj[p].size(); i++)
            //     {
            //         int to = adj[p][i];
            //         if (dis[to] == k - 1)
            //         {
            //             ans.push(to);
            //             break;
            //         }
            //     }
            //     k--;
            // }

            // reverse(res.begin(), res.end());

            // for (auto y : res)
            // {
            //     cout << y << " ";
            // }

            // return;